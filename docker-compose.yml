services:
  easyproxy:
    build: ghcr.io/realbestia1/easyproxy:latest
    container_name: easyproxy
    restart: unless-stopped
    ports:
      - "7860:7860"
    environment:
      #- API_PASSWORD=
      - PORT=7860
    volumes:
      - ./data:/data
